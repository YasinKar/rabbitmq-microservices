version: '3.8'

services:
  redis:
    image: redis:7.0.11-alpine
    container_name: redis
    networks:
      - private_network
    volumes:
      - redis_data:/data
    command: ["redis-server", "--requirepass", "mysecurepassword"]
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin
    networks:
      - private_network

  orders_db:
    image: postgres
    container_name: orders_db
    environment:
      - POSTGRES_NAME=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - orders_data:/var/lib/orders_data
    ports:
      - "5432:5432"
    networks:
      - private_network

  products_db:
    image: postgres
    container_name: products_db
    environment:
      - POSTGRES_NAME=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - products_data:/var/lib/products_data
    ports:
      - "5433:5432"
    networks:
      - private_network

  users_db:
    image: postgres
    container_name: users_db
    environment:
      - POSTGRES_NAME=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - users_data:/var/lib/users_data
    ports:
      - "5434:5432"
    networks:
      - private_network

  orders_service:
    build:
      context: ./services/orders_service
    container_name: orders_service
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./services/orders_service:/app/
    expose:
      - "8000"
    environment:
      - ORDERS_POSTGRES_NAME=postgres
      - ORDERS_POSTGRES_USER=postgres
      - ORDERS_POSTGRES_PASSWORD=postgres
      - ORDERS_POSTGRES_HOST=orders_db
      - ORDERS_POSTGRES_PORT=5432
      - SECRET_KEY=secret
      - DEBUG=1
      - ALLOWED_HOSTS=*
    depends_on:
      - rabbitmq
      - orders_db
    networks:
      - private_network

  products_service:
    build:
      context: ./services/products_service
    container_name: products_service
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8001"
    volumes:
      - ./services/products_service:/app/
    expose:
      - "8001"
    environment:
      - PRODUCTS_POSTGRES_NAME=postgres
      - PRODUCTS_POSTGRES_USER=postgres
      - PRODUCTS_POSTGRES_PASSWORD=postgres
      - PRODUCTS_POSTGRES_HOST=products_db
      - PRODUCTS_POSTGRES_PORT=5432
      - SECRET_KEY=secret
      - DEBUG=1
      - ALLOWED_HOSTS=*
    depends_on:
      - rabbitmq
      - products_db
    networks:
      - private_network

  users_service:
    build:
      context: ./services/users_service
    container_name: users_service
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8002"
    volumes:
      - ./services/users_service:/app/
    expose:
      - "8002"
    environment:
      - USERS_POSTGRES_NAME=postgres
      - USERS_POSTGRES_USER=postgres
      - USERS_POSTGRES_PASSWORD=postgres
      - USERS_POSTGRES_HOST=users_db
      - USERS_POSTGRES_PORT=5432
      - SECRET_KEY=secret
      - DEBUG=1
      - ALLOWED_HOSTS=*
    depends_on:
      - rabbitmq
      - users_db
    networks:
      - private_network

  auth_queue:
    build:
      context: ./services/users_service
    container_name: auth_queue
    command: "python manage.py auth_queue"
    volumes:
      - ./services/users_service:/app/
    environment:
      - USERS_POSTGRES_NAME=postgres
      - USERS_POSTGRES_USER=postgres
      - USERS_POSTGRES_PASSWORD=postgres
      - USERS_POSTGRES_HOST=users_db
      - USERS_POSTGRES_PORT=5432
      - SECRET_KEY=secret
      - DEBUG=1
      - ALLOWED_HOSTS=*
    depends_on:
      - rabbitmq
      - users_db
      - users_service
    networks:
      - private_network
         
  gateway:
    build:
      context: ./gateway
    container_name: gateway
    command: "python manage.py runserver 0.0.0.0:80"
    volumes:
      - ./gateway:/app/
    ports:
      - "80:80"
    environment:
      - SECRET_KEY=secret
      - DEBUG=1
      - ALLOWED_HOSTS=*
      - CORS_ORIGIN_WHITELIST=http://localhost
    depends_on:
      - rabbitmq
      - users_db   
    networks:
      - private_network

volumes:
  redis_data:
  rabbitmq_data:
  orders_data:
  products_data:
  users_data:

networks:
  private_network:
    driver: bridge